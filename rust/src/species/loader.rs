// Species loader - will be fully implemented in Phase 2
pub struct SpeciesLoader;

impl SpeciesLoader {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SpeciesLoader {
    fn default() -> Self {
        Self::new()
    }
}
